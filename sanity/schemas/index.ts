/**
 * Sanity Schema Index
 * Exports all schemas for Sanity Studio
 */

import motorcycle from './motorcycle';
import modelPage from './modelPage';
import homepage from './homepage';
import globalSettings from './globalSettings';

export const schemaTypes = [
  motorcycle,
  modelPage,
  homepage,
  globalSettings,
];

